<div class="container w-xxl w-auto-xs">
  <a href class="navbar-brand block m-t"></a>
  <%= link_to '<span class="hidden-folded text-1x">EduHelper - Admin Login</span>'.html_safe, admin_root_path, class: "navbar-brand block m-t" %>
  <div class="m-b-lg">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="text-danger wrapper text-center">
        <div class='row'>
          <%= render 'admin/partials/flash_message' %>
        </div>
      </div>
      <div class="list-group list-group-sm">
        <div class="list-group-item">
          <%= f.email_field :email, class: 'form-control no-border', placeholder: "Email Address" %>
        </div>
        <div class="list-group-item">
           <%= f.password_field :password, autocomplete: "off", class: 'form-control no-border', placeholder: "Password" %>
        </div>
      </div>

      <button type="submit" class="btn btn-lg btn-primary btn-block">Log in</button>

    <% end %>
  </div>
</div>
